[project]
name = "cursoPython"
version = "0.1.0"

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["entities*", "biblioteca*"]

[tool.pytest.ini_options]
addopts = "-v -s"
